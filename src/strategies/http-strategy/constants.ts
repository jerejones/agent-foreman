/**
 * HTTP Strategy Constants
 */

/** Default timeout for HTTP requests (30 seconds) */
export const DEFAULT_TIMEOUT = 30000;

/** Default allowed hosts for SSRF prevention */
export const DEFAULT_ALLOWED_HOSTS = ["localhost", "127.0.0.1", "::1"];
